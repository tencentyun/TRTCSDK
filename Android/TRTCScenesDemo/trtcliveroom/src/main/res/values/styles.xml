<resources>
    <style name="AppBaseTheme" parent="android:Theme.Black.NoTitleBar">
    </style>

    <!-- 自定义loading dialog -->
    <style name="loading_dialog" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="liveroom_dialog_fragment" parent="@android:style/Theme.Holo.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

    <!--video model -->
    <style name="LiveRoomDialogTheme" parent="Theme.AppCompat.DayNight.Dialog.Alert"/>

    <!-- Input dialog theme. -->
    <style name="InputDialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">#00000000</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
<!--        <item name="colorControlNormal">@color/colorAccent</item>-->
<!--        <item name="android:textColorHint">@color/c9</item>-->
<!--        <item name="colorControlActivated">@color/colorAccent</item>-->
    </style>

    <!--BeautyPanelStyleStart-->
    <style name="BeautyTheme" parent="BeautyPanelStyle">
        <item name="beautyPanelColorPrimary">@color/colorLiveRoomAccent</item>
    </style>

    <style name="link_mic_style">
        <item name="android:layout_width">120dp</item>
        <item name="android:layout_height">180dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:layout_marginRight">8dp</item>
        <item name="android:layout_marginBottom">5dp</item>
    </style>
    <!--BeautyPanelStyleEnd-->
</resources>
